@model IEnumerable<PortfolioBlog.Models.Project>
@{
    ViewData["Title"] = "Projects";
}
<h1>@ViewData["Title"]</h1>

<ul>
@foreach (var model in Model)
{
    
    <li>@Html.ActionLink(model.Title.Remove(0, 8).Replace('-',' '), "ProjectReadMe", "Projects", model)</li>
}

</ul>
